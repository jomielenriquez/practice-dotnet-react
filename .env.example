# Copy to .env and change the password. .env is gitignored.
#
# SQL Server enforces a strong SA password: at least 8 characters drawn from
# three of { uppercase, lowercase, digits, symbols }. If the password is too
# weak the container starts and then exits — check `docker compose logs mssql`.
MSSQL_SA_PASSWORD=Local_Dev_Passw0rd!
